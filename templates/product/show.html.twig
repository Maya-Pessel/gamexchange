{% extends 'base.html.twig' %}

{% block title %} 🧸 {{ product.title}} | gamexchange{% endblock %}

{% block body %}
    <div class="box">
        <div class="col-md-4 mt-4 p-4 mx-auto box yellow-box">
            <h1>{{ product.title}}</h1>
            <img class="w-100" src="{{ (product.image ? vich_uploader_asset(product): asset('images/placeholder.png'))|imagine_filter('squared_thumbnail_medium') }}" alt="{{ product.title }}">
            <p class="mt-2">{{ product.description}}</p>

            <p class="mt-2">Posté par {{product.user.firstName}} {{product.user.lastName}}</p>
            <a href="" class="btn blue-box">
                Proposer un échange
            </a>
            <div class="pt-3">
                {% if  app.user == product.user %}
                    <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="btn btn-primary">Modifier</a>
                {% endif %}
            </div>
        </div>
    </div>
    <div class="mt-6"></div>

{% endblock %}
