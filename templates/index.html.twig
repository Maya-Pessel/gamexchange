{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<style>

</style>

    <div class="row mx-3 justify-content-center">
        {% for product in products %}
            <div class="product yellow-box col-md-3">
                <p class="text-center"><img src={{ asset('build/images/exchange.png') }}></p>
                <h2>{{ product.title}}</h2>
                {# <p>{{ product.description}}</p> #}
            </div>
        {% endfor %}
    </div>

{% endblock %}
